# TripoSH Bispectrum Pipeline Factory

Assuming `git`, `make` and `conda` are available, run in a shell:
```
git clone git@github.com:ROEcosmology/TripoSH-Factory.git
cd TripoSH-Factory
./install_wizard.sh
```
